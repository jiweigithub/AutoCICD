/**
 * Messaging package — NATS JetStream removed in v2.
 * Pipeline communication uses direct API calls through OpenClaw.
 * No message broker needed.
 *
 * Kept as empty module for import compatibility.
 */
